# AGENTS.md

## 목적

이 문서는 `lam` 프로젝트에서 Codex 또는 유사한 코딩 에이전트가 작업할 때 따라야 할 공통 작업 규칙과 프로젝트 전용 규칙을 정의한다.

`lam`은 바 `lam`을 위한 모바일 우선 QR 메뉴 서비스 모노레포이며, `lam-web`과 `lam-api`를 함께 포함한다.

---

## 공통 작업 원칙

코드 관련 요청 시 다음 원칙을 항상 따른다.

1. 내가 명시적으로 "수정", "패치", "적용", "전체 수정" 등을 요청하기 전에는 기존 소스를 임의로 수정하지 않는다.  
   대신 변경이 필요한 부분, 변경 이유, 수정 코드를 제안하는 방식으로 답변한다.

2. 내가 "바로 수정해", "전체 수정해", "패치해", "적용해" 등의 명시적인 요청을 하면 즉시 수정된 코드를 제공한다.  
   전체 파일은 내가 요청한 경우에만 제공한다.

3. 현재 수정 대상으로 지정한 파일만 우선 확인한다. 공통 컴포넌트·유틸리티는 사용자가 요청하거나 현재 파일에서 참조가 확인되는 경우에만 확인한다.

4. 기존 프로젝트의 구조, 네이밍 규칙, 코딩 스타일, 아키텍처를 최대한 유지하며 작업한다.

5. 요청한 범위를 벗어난 리팩터링, 코드 정리, 네이밍 변경, 포맷 변경은 하지 않는다.  
   필요한 부분만 최소 범위로 수정한다.

6. 수정 사항을 제공할 때는 다음 순서로 설명한다.
   - 변경 이유
   - 영향 범위
   - 수정된 코드
   - 적용 시 주의사항

7. 기존 구현 방식이나 공통 규칙이 확인된 경우 이를 우선 사용한다.  
   확인되지 않은 경우에는 존재를 추측하지 않는다.

8. 필요한 파일이 없으면 먼저 파일을 요청한다. 추측해서 프로젝트를 탐색하지 않는다.

9. 전체 파일을 요청하지 않은 경우에는 변경된 부분만 제공한다.

10. 기존 코드의 문제를 질문한 경우에는 수정본을 바로 작성하지 말고 원인과 해결 방법을 먼저 설명한다.

11. 답변 전 다음 사항만 검토한다.
    - 사용자의 요구사항을 모두 반영했는가
    - 기존 기능을 깨뜨리지 않는가
    - 불필요한 변경은 없는가
    - 요청 범위를 벗어나지 않았는가

12. 검토 결과 문제가 발견되면 먼저 수정한 후 최종 답변을 제공한다.

13. 추측으로 답변하지 않는다.  
    확실하지 않은 내용은 명확하게 불확실하다고 설명하고 필요한 정보를 요청한다.

14. 성능, 유지보수성, 안정성을 고려하되 기존 코드베이스와 가장 자연스럽게 어울리는 구현을 우선 제안한다.

15. 프로젝트 전체 분석, 전체 검색, 와일드카드 검색(`grep`, `find` 등)은 절대 수행하지 않는다. 사용자가 파일명을 지정한 경우 해당 파일만 작업한다. 추가 파일이 반드시 필요하면 먼저 요청한다.

16. 이미 제공된 파일, 코드, 로그, 프로젝트 구조를 우선 활용한다.  
    동일한 정보를 다시 탐색하거나 재분석하지 않는다.  
    새로운 정보가 필요한 경우에만 추가 확인을 수행한다.

17. 수정 대상 파일 외의 파일은 읽지 않는다.  
    반드시 필요한 경우에만 추가 파일을 확인하고, 먼저 이유를 설명한다.

18. 빌드, 테스트, 린트, 패키지 설치는 명시적으로 요청한 경우에만 수행한다.

19. 수정 가능한 범위가 여러 파일이라도 우선 현재 파일만 수정안을 제시한다.  
    다른 파일 수정이 필요한 경우 이유를 먼저 설명한다.

---

## 프로젝트 개요

이 프로젝트는 바 `lam`을 위한 모바일 우선 QR 메뉴 서비스 모노레포다.

구성:
- `lam-web`: 손님용 모바일 웹과 관리자 UI
- `lam-api`: 메뉴/공지/요청/관리 기능용 API

손님은 QR 스캔 후 모바일 웹에서 메뉴를 보고, 요청을 남기고, 이벤트/공지 내용을 확인한다.  
운영자는 관리자 화면에서 카테고리, 메뉴, 요청, 이벤트를 관리한다.

---

## 프로젝트 전용 작업 규칙

### 1. 구조 이해

- 프론트엔드는 `lam-web`
- 백엔드는 `lam-api`
- 손님 화면과 관리자 화면은 같은 서비스 도메인을 공유하지만 역할이 다르므로 혼동하지 않는다.
- 프론트와 백엔드가 연결된 구조라도, 사용자가 지정한 파일 범위를 우선한다.

### 2. 손님 화면 규칙

- 손님 화면은 모바일 우선 UX를 유지한다.
- 메뉴, 요청, 이벤트 흐름은 빠르고 단순해야 한다.
- 불필요하게 관리자 개념이 손님 화면에 섞이지 않도록 한다.
- 문구 수정 시에는 안내문보다 실제 사용자가 바로 이해할 수 있는 자연스러운 표현을 우선한다.

### 3. 관리자 화면 규칙

- 관리자 화면은 운영 편의성이 우선이다.
- 메뉴 관리, 요청 확인, 이벤트 관리 흐름을 깨지 않도록 한다.
- 목록 밀도, 버튼 크기, 스크롤 방식 등은 실제 운영 화면처럼 효율적으로 유지한다.
- 확인/처리/삭제 같은 상태 액션은 현재 정의된 운영 방식에 맞춘다.

### 4. 요청 기능 규칙

손님 요청은 현재 두 종류로 나뉜다.

- `바로 전달하기`
- `특별한`

규칙:
- 두 요청 타입을 다시 하나로 합치지 않는다.
- 손님 화면에서는 두 요청 타입이 분리된 UX를 유지한다.
- 관리자 화면에서도 요청 보드를 분리된 단위로 유지한다.

### 5. 데이터 모델 규칙

현재 요청 데이터 구조는 분리되어 있다.

- 일반 요청: `customer_requests`
- 특별 요청: `special_requests`

규칙:
- 특별 요청 관련 필드를 다시 일반 요청 테이블에 섞지 않는다.
- 특별 요청 전용 입력값은 특별 요청 모델 기준으로 유지한다.
- nullable 처리나 스캔 에러가 발생하지 않도록 현재 분리 구조를 우선 존중한다.

### 6. 특별 요청 규칙

특별 요청은 일반 요청과 달리 구조화된 폼 기반이다.

현재 필드:
- 성별
- 이름
- 나이
- 사는 곳
- 연락처(인스타그램)
- 이상형
- 하고 싶은 말

규칙:
- 특별 요청은 장부형/상세형 관리 흐름을 유지한다.
- 관리자에서는 `남자`, `여자` 분리 구조를 유지한다.
- 특별 요청은 상태 처리보다 상세 확인과 삭제 중심 흐름을 우선한다.
- 특별 요청 관련 UI는 일반 요청 UI와 동일하게 단순 복제하지 않는다.

### 7. 메뉴 관리 규칙

- 메뉴는 카테고리 기준으로 관리한다.
- 현재 프로젝트 구조와 UI 흐름에 없는 과도한 관리 기능을 임의로 추가하지 않는다.
- 메뉴 이미지, 대표 이미지, 크롭 흐름이 있는 경우 기존 방식을 우선 따른다.
- 사용자가 원하지 않은 전체 필터, 과도한 옵션, 불필요한 제어는 추가하지 않는다.

### 8. 이벤트/공지 규칙

- 이벤트/공지는 관리자에서 작성하고 손님 화면에 노출되는 운영 정보다.
- 실제 서비스 성격에 맞지 않는 명칭이나 과도한 안내성 문구는 지양한다.
- 이미 사용자 의도에 따라 성격이 바뀐 영역은 과거 명칭으로 되돌리지 않는다.

### 9. UI 작업 규칙

- 모바일 화면에서는 세로 길이와 밀도를 특히 주의한다.
- 카드, 버튼, 리스트는 사용자가 명시한 밀도와 크기를 우선 반영한다.
- 스크롤이 필요한 경우 페이지 전체 스크롤과 내부 스크롤을 구분해서 처리한다.
- 슬라이드 UI가 있는 경우 현재 활성 위치를 사용자가 직관적으로 알 수 있어야 한다.
- 시각적으로 과하게 커지거나, 운영 화면에서 비효율적인 배치는 피한다.

### 10. README / 문서 수정 규칙

- README는 현재 실제 기능 기준으로 설명한다.
- “추후 예정”, “스캐폴드 수준” 같은 오래된 설명이 실제 구현과 다르면 현재 상태 기준으로 갱신한다.
- 기능 소개는 단순 나열보다 사용자 흐름과 운영 흐름이 드러나도록 자연스럽게 작성한다.

### 11. API / 백엔드 수정 규칙

- 프론트 변경으로 인해 백엔드 수정이 필요하면 그 이유를 먼저 설명한다.
- 기존 엔드포인트를 함부로 합치거나 이름을 바꾸지 않는다.
- 입력 구조가 달라질 때는 프론트/백엔드/DB 영향 범위를 함께 고려한다.
- 스키마 변경은 현재 실제 사용 중인 기능 구조를 반영하는 방향으로만 최소 적용한다.

### 12. 검증 규칙

- 사용자가 요청하지 않은 한 `build`, `lint`, `test`, 패키지 설치를 실행하지 않는다.
- 검증을 하지 못한 경우 최종 답변에서 명확히 밝힌다.
- 검증이 필요한 변경인데 실행 요청이 없으면, 실행하지 않은 상태임을 분명히 적는다.

---

## 우선순위

작업 중 충돌이 있을 경우 아래 우선순위를 따른다.

1. 사용자가 현재 턴에서 직접 요청한 사항
2. 이 문서의 공통 작업 원칙
3. 이 문서의 프로젝트 전용 작업 규칙
4. 기존 코드베이스의 구현 방식과 스타일

---

## 요약

이 프로젝트에서 에이전트는 다음 원칙을 가장 중요하게 지켜야 한다.

- 함부로 수정하지 않는다.
- 지정된 파일만 본다.
- 추측하지 않는다.
- 최소 범위만 수정한다.
- 현재 기능과 운영 흐름을 깨지 않는다.
- 손님 화면과 관리자 화면의 역할을 혼동하지 않는다.
- 요청 기능의 분리 구조를 유지한다.
