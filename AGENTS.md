# 저장소 작업 지침

## 프로젝트 구조

- `lam-web`: 손님용 모바일 QR 메뉴 웹
- `lam-admin-web`: 운영자용 Next.js 관리자 웹
- `lam-api`: 손님·관리자 기능을 제공하는 Go API
- 존재하지 않는 디렉터리, 기능, API 계약은 구현된 것으로 가정하지 않는다.

## 기준 규칙

- 파일을 수정하기 전에 루트와 변경 경로에서 가장 가까운 `AGENTS.md`를 읽는다. 하위 문서는 해당 디렉터리에만 추가 적용된다.
- 실제 코드와 프로젝트 문서에서 구조와 계약을 확인하고 추측하지 않는다.
- 사용자가 수정·패치·적용을 요청하기 전에는 파일을 변경하지 않는다.
- 지정된 파일과 직접 연결된 참조만 우선 조사하고, 추가 확인이 필요하면 이유를 먼저 알린다.
- 무관한 리팩터링, 일괄 포맷 변경, 다른 개발자의 변경 덮어쓰기를 하지 않는다.

## 아키텍처

- 손님 기능은 `lam-web`, 운영 기능은 `lam-admin-web`에 둔다.
- 손님과 관리자의 화면, 라우트, 인증 흐름을 섞지 않는다.
- 프론트엔드 API 접근은 확인된 service 또는 API 계층에 둔다.
- 인증, 인가, 객체 소유권은 서버에서 강제하며 UI 숨김을 보안 통제로 사용하지 않는다.
- 일반 요청(`customer_requests`)과 특별 요청(`special_requests`)의 API, 저장, UI 흐름을 합치지 않는다.
- 적용된 DB migration은 수정하지 않고 새 migration을 추가한다.
- 공개 API 계약을 변경하면 영향을 받는 프론트엔드와 현재 상태 문서를 함께 확인한다.

## 작업 절차

- 한 변경은 검증 가능한 하나의 목표에 집중한다.
- 기능, 버그 수정, 리팩터링, 실행 동작 변경에는 `lam-work-on-issue` Skill의 TDD와 완료 전 검증을 적용한다.
- 브랜치 생성·전환, commit, push, PR, merge는 사용자가 요청한 범위에서만 수행한다.
- 사용자가 요청하지 않았고 지속적인 필요가 없다면 설계 문서, 구현 계획, ADR, changelog, 작업 일지를 만들지 않는다.
- 커밋을 요청받으면 `type: 한글 변경사항` 형식을 사용한다. Issue 번호가 확인된 작업은 본문에 `Refs: #번호`를 넣는다.

## 브랜치

- 개발자는 2명이며 각자 자신의 dev 브랜치(`ino/dev`, `ms/dev`)를 갖는다. 각 dev 브랜치는 해당 개발자 작업의 기준(base/root)이자 장기 통합 브랜치다. 실험적이거나 아직 정리되지 않은 커밋이 섞일 수 있다.
- 작은 격리 변경은 본인 dev 브랜치에서 수행할 수 있다.
- 장기, 병렬, 고위험, 공개 API, DB migration, 인증·인가 변경은 최신 본인 dev 브랜치에서 작업용 branch를 분기하고 완료 후 본인 dev 브랜치로 병합한다. 이 작업용 branch는 `main`으로 직접 전달하지 않는다.
- `main`으로 전달할 때는 본인 dev 브랜치에서 `main`으로 직접 Pull Request를 연다. cherry-pick이나 전달 전용 별도 브랜치를 만들지 않는다. 전달 작업에는 `lam-deliver-change` Skill을 사용한다.
- PR 병합 후 본인 dev 브랜치를 최신 `main` 기준으로 동기화한다.
- 다른 개발자의 dev·feature 브랜치와 담당 영역은 확인된 정보만 사용한다. 다른 개발자 소유 브랜치에 커밋·push하기 전에는 사용자의 명시적 지시를 확인한다.
- 사용자 승인 없이 force push, history rewrite, destructive migration, 자동 merge를 수행하지 않는다.

## TDD와 검증

- 동작 변경은 실패하는 테스트를 먼저 작성하고 예상한 이유로 실패하는 RED를 확인한다.
- 최소 구현으로 GREEN을 만든 뒤 필요한 리팩터링과 영향받는 회귀 검증을 수행한다.
- 테스트가 처음부터 통과하거나 환경 문제로 실패한 경우 RED로 보고하지 않는다.
- 완료, 수정 성공, 테스트 통과를 주장하기 전에 그 주장을 증명하는 최신 명령과 결과를 확인한다.
- 실행하지 않은 검증을 통과했다고 표현하지 않는다.

### 단순 변경 예외

다음 조건을 모두 만족하면 자동 테스트, 전체 빌드, 전체 린트를 생략할 수 있다.

- 런타임 동작, API, DB, 인증, 의존성, 빌드 설정에 영향이 없다.
- 문구, 주석, 순수 문서, 비동작 메타데이터처럼 결과를 diff에서 직접 확인할 수 있다.
- 다른 개발자의 작업 또는 공통 계약과 충돌하지 않는다.

단순 변경도 diff를 확인하고 검증 생략 이유를 보고한다. CSS, 라우팅, 환경변수, 설정, 타입 변경은 단순 변경으로 간주하지 않는다.

## 보안

- secret, 실제 개인정보, 비밀번호, access token, refresh token, private key를 커밋하거나 로그에 남기지 않는다.
- HTTP 입력, 업로드 파일과 파일명, 렌더링 HTML을 신뢰하지 않는다.
- DB query는 parameter binding을 사용한다.

## 프로젝트 Skill

- Issue 또는 코드 변경 작업: `.agents/skills/lam-work-on-issue/SKILL.md`
- 본인 dev 브랜치에서 `main`으로 변경 전달: `.agents/skills/lam-deliver-change/SKILL.md`

RBAC 전용 Skill은 두지 않는다. 권한 변경도 `lam-work-on-issue`와 이 문서의 보안 규칙을 따른다.

## 완료 보고

- 변경 이유
- 영향 범위와 변경 파일
- 실행한 검증과 결과
- 생략한 검증과 이유
- 남은 위험 또는 적용 시 주의사항
